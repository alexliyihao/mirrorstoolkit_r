# Multi-column Summary

Wrapper function output multiple column base::summary() result into
data.frame I really cannot understand why R returns that in that weird
way.

## Usage

``` r
multi_line_summary(data)
```

## Arguments

- data:

  data.frame, the data.frame working on

## Value

data.frame, the cleaned result
